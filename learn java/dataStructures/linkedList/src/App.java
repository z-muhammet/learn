import java.io.IOException;

public class App {
    public static void main(String[] args) throws IOException {
        int select = 0;
        while (select != 5) {
            System.out.printf("pls u select (add prepend (1), add last (2),  ");

        }
    }
}
